@page "/fetchdata"
@using BlazorApp1_client_javascript.Shared
@inject HttpClient Http

<div>
    @((MarkupString)val)
</div>

@code {
    private WeatherForecast[]? forecasts;
    private HttpResponseMessage? output;
    private HttpResponseMessage? output1;
    string val;
    string val2;
    protected override async Task OnInitializedAsync()
    {

        output = await Http.GetAsync("/counter");

        var tes = output.Content.ReadAsStringAsync();
        var tes1 = tes;
        val = tes1.Result;
    }
}
